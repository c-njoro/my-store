this is the backend to the repo called userend, they run together;
first run this backend the the frontend


add .env file on the back end and add the following:

#local mongo db
LOCAL_KEY=URI of your local mongodb 

#port
PORT=3000(the port you want the backend to run on)
